- [x] Phase 1 complete through step 1.4 (Backend fixes + Clerk auth + clerk_id linking).
- [x] Phase 2.1 Install Zod and add reusable validate middleware (`src/middleware/validate.ts`).
- [x] Phase 2.2 Add schemas for all write endpoints (`/enrollments`, `/users`, `/users/:id`, `/users/:id/status`, `/users/sync`).
- [x] Phase 2.3 Standardize error format and add shared API error helper.
- [x] Phase 3 Set up `node-pg-migrate` and convert SQL files to migration scripts.
- [x] Phase 4 Add missing detail endpoints and pagination for list routes.
- [x] Phase 5 Add audit log table + write tracking on all mutations.
- [x] Phase 6.1 Frontend app shell + role-aware navigation (`ADMIN`, `ES`, `AS`).
- [x] Phase 6.2 ADMIN views: users/status management + audit log screen + maintenance overview.
- [x] Phase 6.3 ES views: language selection, status controls, active assignments/offers.
- [x] Phase 6.4 AS views: create transfer request (premise_id + timeslot), tracking history, completion action.
- [x] Phase 6.5 Backend support for frontend role UX (ownership-scoped endpoints + stricter role guards).
- [x] Sanity 1: fix frontend endpoint contract mismatch by making dashboard requests role-aware.
- [x] Sanity 2: include admin audit-log loading in dashboard data path.
- [x] Sanity 3: pin Node runtime requirement (`.nvmrc`, package `engines`) for frontend/backend consistency.
- [x] Sanity 4: add backend `.env.example` for predictable environment setup.
- [x] Sanity 5: add migration status check command (`npm run migrate:status`).
- [x] Sanity 6: add auth/role smoke check command (`npm run smoke:auth`) for 401/403/200 validation.
- [x] Sanity 7: cleanup commit hygiene (`.gitignore` hardening for deps/build/backups).
- [ ] Phase 7 Add automated tests (dispatch fairness, race conditions, auth/role + core frontend flows).
- [ ] Phase 8 Railway deployment pipeline + health monitoring + runbook checks.

## Post-Commit QA Checklist (after `271ec3f`)

- [x] Apply latest migrations and confirm `009_add_current_enrollment_pointer` is present.
- [x] Admin `Enrollment Queue Control`: verify search/filter/aging badges behave correctly.
- [x] Admin manual re-offer: only `WAITING` enrollments can be re-offered; action logs in audit.
- [x] AS `Assigned / Offered ES`: verify name/id appears before and after acceptance.
- [x] AS `ES Work Status` badges: verify active/queued/waiting/blocked/completed states display correctly.
- [x] ES `Set Current`: verify AS sees whether ES is working this enrollment or another one.
- [x] All-ES-rejected scenario: AS sees "schedule another day" guidance and blocked status.
- [x] Completion ownership: AS cannot complete; ES can complete own assigned; admin override still works.
- [x] Conflict prevention UX: modal confirms before reject/complete/role change; cancel performs no action.
- [x] Session awareness: invalid/expired session shows warning banner.
- [ ] Sound settings: `Sound: On/Off` persists after reload and toggles notifications.
- [x] ES new-offer alert: plays ~5 seconds on newly received pending request.
- [ ] AS transition sounds: accepted/reassigned/all-rejected/completed each trigger expected sound.
- [ ] Polling refresh: AS/ES dashboards update in ~10s without manual refresh.
- [ ] Regression checks: backend `npm run typecheck`, frontend `npm run build`, backend `npm run smoke:auth` pass.
